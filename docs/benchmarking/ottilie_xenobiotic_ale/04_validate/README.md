# 04_validate — validation & benchmarking tooling

Scripts live flat here; each analysis writes into its own results directory. Convention:
`run_*.sh` = launcher (records exactly how a result was produced), `*.py` = the analysis itself.
Committed results are the summary tables and notes; bulky per-sample intermediates are left
untracked and regenerated by re-running the launcher.

## SNV / INDEL

| Script | Purpose | Results |
|---|---|---|
| `snv_indel_concordance.py` | Score calls against the 1,405-mutation truth set (Ottilie Sup. Data 4); `--pass-only` measures the soft filter's cost | `tier2_results/`, `pilot_results_v2/`, `pilot_results_v3/` |
| `run_validate_all_pilot_v3.sh` (and `_v2`) | The whole pilot suite in one go: `validate_all.py` + PASS-only + soft-filter PASS rates; v3 = v2 with only the output/results dirs changed | `pilot_results_v3/` (current recipe, 2026-09-09) · `pilot_results_v2/` (untrimmed, hard filter on) |
| `validate_all.py` | Full suite (SNV/INDEL + CNV + SV) → `VALIDATION_REPORT.md` | `pilot_results*/`, `tier2_results/` |
| `investigate_missed_snv_indel.sh` | Why a truth mutation was missed (depth/MAPQ at the site) | prints |
| `run_snv_indel_concordance_tier2.sh`, `run_validate_all_{pilot_v2,tier2}.sh` | Launchers for the above | |

## SV

| Script | Purpose | Results |
|---|---|---|
| `make_cohort_samplesheet.py` | Build the CRAM samplesheet for a cohort size / experiment-grouping run (encodes the two rules that cost a run each: `experiment` must match the CRAMs' `@RG SM`; the parent can be in only one group) | feeds the two below |
| `run_sv_mode_series.sh` | Run one cohort through **all three Manta modes** (per-sample / joint / joint+high-sensitivity) | `output_*_{persample,jointdefault,joinths}` |
| `compare_sv_pass_tables.py` | Score those merged pass tables — parent presence, clone-specific rows, and which are **false** (engineered-cassette loci) | feeds `manta_joint_at_scale/REPORT.md` |
| `manta_joint_vs_single.py` | Per-record audit of joint vs per-sample Manta (what the joint call loses/gains) | `manta_joint_at_scale{,_hs}/` |
| `run_manta_joint_at_scale.sh` | Launcher for the 86-sample audits | same |
| `run_manta_joint_audit_pilot.sh` | The 4-sample mode sweep (`default`/`exome`/`noedgecap`/`both`/`cap30`) | `pilot_results_v2/manta_joint_audit/` |
| `sv_cohort_matrix.py`, `sv_characterization.py`, `run_sv_matrix_pilot.sh` | Cohort SV matrix + event characterisation | `pilot_results*/` |
| `sv_merge_bench/` | SVDB merge-recipe benchmark (chose the current chain) | in-dir NOTES.md |

**Start here for SV mode questions:** [`manta_joint_at_scale/REPORT.md`](manta_joint_at_scale/REPORT.md)
(4/16/48/86-sample comparison) and the user-facing summary in
[`docs/variant-calling/manta/manta_calling_modes.md`](../../../variant-calling/manta/manta_calling_modes.md).

## CNV

| Script | Purpose | Results |
|---|---|---|
| `cnv_concordance.py`, `cnv_concordance_pilot.py` | CNV calls vs expected events | `pilot_results*/`, `tier2_results/` |
| `cn_cohort_matrix.py`, `run_cn_bins_{pilot,tier2}.sh` | Cohort CN matrices / bin-level CN | `*_results/` |
| `cnvkit_ploidy_experiment/` | Why CNVKit's integer `cn` ignores `--ploidy` | in-dir `ploidy_comparison.md` |

## Shared

`sample_names.py` — maps paper clone names ↔ pipeline sample names (`data/ottilie/sample_name_dictionary.csv`).
`VALIDATION_PLAN.md` — what validation is expected to cover. `MUTATION_REPORT_PLAN.md` — report design notes.

## Cohorts used in the SV mode comparison — and how to rebuild each

Every cohort the SV comparison ran on is reconstructible; the mechanism differs by cohort. Verified
2026-09-07 (the 16/48/86 sheets regenerate byte-for-byte against the ones actually used).

| Cohort | Defined by | Rebuild |
|---|---|---|
| 2-sample (chr subset) | `data/ottilie/samplesheet_test.csv` | Re-run [`01_data_retrieval/release/generate_test_data.sh`](../01_data_retrieval/release/generate_test_data.sh) — it writes the sheet |
| 4-sample pilot | `data/ottilie/samplesheet_pilot.csv` | ⚠️ no generator: the 4 samples + SRA accessions are listed in [`release/upload_pilot_data.sh`](../01_data_retrieval/release/upload_pilot_data.sh) and [`DATA_PROVENANCE.md`](../DATA_PROVENANCE.md) |
| 16 / 48 / 86 | Tier-2 `md.cram`s, parent + first N−1 clones alphabetically | `make_cohort_samplesheet.py --n 16` (omit `--n` for all 86) |
| 2 experiment groups | same, split by `--groups` | `make_cohort_samplesheet.py --n 16 --groups 2` |

`data/ottilie/*` is gitignored, so the sheets themselves are not in the repo — the generators are.

### ⚠️ A sample list is not a rebuild — record the preprocessing too

The table above rebuilds **which samples**, not **how their reads were processed**, and for these
cohorts the two differ. The Tier-2 CRAMs behind the 16/48/86/2-group runs were produced **2026-06-01
with `trim_fastq: false` and no fastp step at all**; the pipeline default changed on **2026-09-04**
(`948163c`) to `--trim_adapter --trim_quality_3prime tail`. Re-running any of these cohorts from
FASTQ today therefore yields *a different experiment on the same samples*, not a reproduction.

| To do this | Pass |
|---|---|
| Reproduce the SV benchmark **as run** | `--trim_adapter false`, leave `trim_quality_3prime` unset |
| Re-cut it against the **current default** | nothing — the default is now trimming |

A re-cut is a legitimate thing to want; it is just not the same number. Expect absolute counts to
move and the architectural findings to hold — see the untrimmed-reads caveat at the top of
[`manta_joint_at_scale/REPORT.md`](manta_joint_at_scale/REPORT.md) for which is which, and why Manta
is more exposed to trimming than most callers. The same exposure is recorded for the Azure baseline
in CLAUDE.md.

**Rule of thumb for any future cohort here:** the reproducibility unit is *sample list + the
parameters that touch reads*, and the second half is the one that silently goes stale when a default
changes.

⚠️ **Selection is alphabetical, and clone names begin with their compound**, so cohorts cluster by
compound (6 of 15 clones at 16 samples are CBR668) while distinct compounds rise with size (7 → 26 →
43 at 16/48/86). Cohort size and clone diversity therefore co-vary; see the confound caveat in
[`manta_joint_at_scale/REPORT.md`](manta_joint_at_scale/REPORT.md) before attributing an effect to
size alone.

## Results directories

`pilot_results/` (4-sample, superseded) · `pilot_results_v2/` (4-sample, untrimmed reads + hard filter on —
`NOTES.md` holds the Manta mode sweep and the two hard sites) · `pilot_results_v3/` (4-sample, **current
recipe**, 2026-09-09 — 41/42 unchanged; `NOTES.md` has the v2→v3 deltas) · `tier2_results/` (86-sample) · `manta_joint_at_scale{,_hs}/` (86-sample joint-vs-single
audits; `summary.tsv` + `REPORT.md` tracked, per-sample `details_*.tsv` not).
