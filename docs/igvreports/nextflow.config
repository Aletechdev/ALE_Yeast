// Config for igvreports generation workflows
docker.enabled = true
docker.runOptions = '-u $(id -u):$(id -g)'

// Limit parallel sample reports to avoid memory pressure on D4as (16 GB)
executor {
    queueSize = 3
}
